One-line action button; sentence-case label, optional leading 16px icon.
```jsx
<Button variant="primary" onClick={save}>Save changes</Button>
<Button variant="secondary" size="sm">Add goal</Button>
```
Variants: primary (one per view), secondary, ghost, danger. Sizes sm/md/lg.