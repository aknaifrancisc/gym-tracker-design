Smoothed line/area chart: gridlines, mono y-ticks, gradient fill, last-value bubble. Width auto-fits container.
```jsx
<TrendChart data={[104.8,104.1,103.6,103.9,102.8]} height={130} showDots xLabels={['Jun','Jul','Aug']} />
```
Series 1 = chart-1 (brand blue). No distortion — renders at measured pixel width.